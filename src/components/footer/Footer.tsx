import React from "react";
import styles from "./footer.module.scss";

const Footer = () => {
  return <footer className={styles.footer}>Todos os diretos reservados</footer>;
};

export default Footer;
